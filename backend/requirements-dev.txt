-r requirements.txt

# Linting y formato (equivalente a ESLint + Prettier en Python)
ruff>=0.9.0,<1.0.0

# Hooks de Git para verificar antes de cada commit
pre-commit>=4.0.0,<5.0.0

# Testing
pytest>=8.3.0,<9.0.0
pytest-asyncio>=0.24.0,<1.0.0
aiosqlite>=0.20.0,<1.0.0
