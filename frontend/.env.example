# Community AI Monitor — Frontend (desarrollo local)
# Copiar a .env si necesitas sobreescribir los valores por defecto.
#
# En desarrollo, si omites estas variables, Vite usa el proxy:
#   /api/v1  →  http://localhost:8000/api/v1
#   ws://localhost:5173/api/v1/ws  →  backend WebSocket
#
# VITE_API_BASE_URL=/api/v1
# VITE_WS_BASE_URL=ws://localhost:5173/api/v1/ws
